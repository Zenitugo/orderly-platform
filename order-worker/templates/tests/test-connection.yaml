apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "order-worker.fullname" . }}-test-connection"
  labels:
    {{- include "order-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "order-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
