apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderly-platform.fullname" . }}
  labels:
    {{- include "orderly-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orderly-platform.selectorLabels" . | nindent 4 }}
