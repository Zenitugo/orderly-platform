apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orderly-platform.fullname" . }}-test-connection"
  labels:
    {{- include "orderly-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orderly-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
